services:
  tonamiibuki:
    build: .
    container_name: tonamiibuki-aiops
    ports:
      - "8000:8000"
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
